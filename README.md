# Git Restore

A simple project created while learning Git Restore.

## What is Git Restore?

`git restore` is used to discard changes in the working directory and restore files to their previous state.

## Example

```bash
git restore index.html
